---
import Doc from '../../layouts/Doc.astro';
import { getDocs } from '../../lib/docs';

export function getStaticPaths() {
  return getDocs().map((entry) => ({
    params: { slug: entry.param },
    props: { entry },
  }));
}

const { entry } = Astro.props;
---

<Doc entry={entry} />
